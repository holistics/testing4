use datasets.test {ecommerce_test_modules}
use models {users, cities, countries, orders, order_items, products}

Dataset prod_ecommerce = ecommerce_test_modules.extend({
  label: 'Prod Ecommerce (Module)'
})